[project]
name = "imessage-to-sqldb-tools"
version = "0.1.0"
description = "Optional Python tooling for local iMessage DB checks in imessage-to-sqldb"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.uv]
package = false
